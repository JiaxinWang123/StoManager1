# Core scientific stack
numpy<2.0           # ensure compatibility with pandas, PyQt, ultralytics
pandas>=2.0,<3.0    # dataframe handling
scipy>=1.10,<2.0    # scientific routines
matplotlib>=3.7,<4.0
openpyxl>=3.1       # Excel support for pandas

# GUI
PyQt5>=5.15,<6.0    # PyQt5 for the UI
qtpy>=2.4           # abstraction layer for Qt

# YOLO / deep learning
torch>=2.0,<3.0     # PyTorch (CPU by default; install CUDA build if GPU available)
torchvision>=0.15,<1.0
ultralytics>=8.0.0,<9.0.0  # YOLOv8 segmentation/detection

# Image processing
opencv-python>=4.8,<5.0    # OpenCV for image I/O
Pillow>=10.0,<11.0         # Pillow for raster image handling

# Utilities
tqdm>=4.65                # progress bars
pyyaml>=6.0               # YAML file support
